# PRODUÇÃO - PostgreSQL Configuration
# Substitua pelos dados reais do seu banco de produção:

DATABASE_URL=postgresql://usuario_postgres:senha_postgres@servidor_postgres:5432/nome_banco_squad

# Exemplo real (substitua pelos seus dados):
# DATABASE_URL=postgresql://squad_user:MinhaSenh@Segur@123@db.exemplo.com:5432/squad_production

# JWT Configuration (use uma chave mais segura em produção)
JWT_SECRET_KEY=chave-jwt-super-secreta-para-producao-change-this-now
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Environment
ENVIRONMENT=production

# Optional: Se usar SSL
# DATABASE_URL=postgresql://usuario:senha@host:5432/database?sslmode=require 